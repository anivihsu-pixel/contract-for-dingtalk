# v2.50.7 交付说明

## 修复范围

本包修复生产 Nginx 环境下登录后回跳地址污染问题，运行时版本号为 `v2.50.7`。

## 修复内容

- 根路径 `/` 直接映射 `Dashboard/index`。
- 尾斜杠统一归一化。
- 登录回跳剔除内部路由参数 `s`。
- 账号密码登录只采用服务端安全校验后的站内回跳地址。
- 钉钉前端回跳只允许单斜杠开头的站内相对路径。

## 功能边界

- 当前版本不包含商机模块。
- 本次无数据库结构变更。
