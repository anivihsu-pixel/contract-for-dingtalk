# 多角色全场景全流程测试报告

- **被测版本**：合同管理系统 v2.47.1（含本次 dashboardSummary 修复）
- **测试日期**：2026-08-11
- **测试环境**：PHP 8.4.22 + ThinkPHP 6.1 + SQLite 演示库（`runtime/data/contract.db`），本地 `http://127.0.0.1:8000`
- **测试方式**：HTTP 真实链路（登录 → 业务操作 → 落库断言），独立 SQL 复算比对，权限矩阵代码核对

---

## 一、测试范围与结果总览

| 测试块 | 覆盖内容 | 结果 |
| --- | --- | --- |
| 1. 权限矩阵核对 | 6 角色 22 项权限抽查 | ✅ 全部符合预期 |
| 2. 统计准确性审查 | 11 项报表指标独立 SQL 复算 | ⚠️ 10 项一致，1 项不一致（已修复） |
| 3. 全流程闭环 E2E | 68 项断言（四场景 + 权限门控） | ✅ 68/68 通过 |
| 4. 发布门禁 | 六项静态检查 + PHPUnit | ✅ 全部通过（65 tests / 172 assertions） |

---

## 二、权限矩阵核对（6 角色 22 项）

从 `user_role`/`role_permission` 代码核对各角色权限码，抽查关键权限点：

| 权限点 | admin | manager | legal | finance | user | gm |
| --- | :-: | :-: | :-: | :-: | :-: | :-: |
| dashboard:company（全公司经营/周报） | ✅ | ❌ | ❌ | ✅ | ❌ | ✅ |
| dashboard:dept | ✅ | ✅ | ❌ | ✅ | ❌ | ✅ |
| contract:create | ✅ | ✅ | ❌ | ❌ | ✅ | ✅ |
| approval:approve（审批动作） | ✅ | ✅ | ✅ | ✅ | ❌ | ✅ |
| payment:create（回款登记） | ✅ | ✅ | ❌ | ✅ | ❌ | ✅ |
| invoice:create（开票） | ✅ | ✅ | ❌ | ✅ | ❌ | ✅ |
| customer:create | ✅ | ✅ | ❌ | ❌ | ✅ | ✅ |

**结论**：角色权限与设计一致（admin/gm=ALL，manager=DEPT，legal=审批+法务，finance=ALL+财务，user=SELF）；`dashboard:company` 仅 admin/gm/finance 可见，与「经营周报总经理可见」设计相符。

---

## 三、统计准确性审查（独立 SQL 复算）

对仪表盘/月报/财务/税务/往来 360/项目毛利/移动报表/账龄/部门排名/个人业绩/经营周报共 **11 项指标**，用与业务逻辑口径一致但独立编写的 SQL 复算，逐字段比对（316 项字段级比对）：

### 3.1 发现并修复的问题（1 项，严重）

**`dashboardSummary()` 状态快照指标周期过滤错误**

- **现象**：仪表盘「状态分布 / 待审批 / 已签执行 / 30 天内到期」四组快照指标，在 `period=month/quarter/year` 时被周期过滤（`effective_date` 在周期内）后的 `$baseQuery` 复用，导致状态卡空白、待审批数与生效合同数恒为 0；仅 `period=all` 正常。
- **根因**：[ReportLogic.php](file:///C:/Users/wow/AppData/Roaming/TRAE%20SOLO%20CN/ModularData/ai-agent/work-mode-projects/6a71e12c2400457f8e662997/contract-review-system-export/contract-review-system/contract-review/app/common/logic/ReportLogic.php) 中状态快照复用被 `effective_date` 周期过滤的 `$baseQuery`——状态快照与新增/回款等「周期增量指标」语义不同，应统计**当前全量快照**（仅数据权限，不随时间周期过滤）。
- **修复**：快照指标改用独立 `$snapBase`（仅 `is_deleted=0` + 数据权限，无周期过滤），`status_counts/pending_approval/signed_contracts/expiring_soon` 四组均从快照口径计算。
- **验证**：注入会话后四档（month/quarter/year/all）分别与独立 SQL 复算，结果全部一致。

### 3.2 其余 10 项指标

仪表盘增量（周期合同额/回款额/新增数）、经营月报、财务中心、税务、往来 360、项目毛利、移动报表、应收账龄、部门排名、个人业绩、经营周报——**字段级比对全部一致**。

### 3.3 潜在口径建议（未触发，需人工确认是否处理）

子代理复核指出 2 处潜在偏差，**演示库数据恰好一致故未触发，本次未修改**：

1. `trend`（季度趋势）合同额未过滤「未生效状态」；
2. `project aggregate / payScope` 回款侧未排除「未生效状态」的合同。

如需彻底对齐「统计统一口径」（交易 `trade_attr=1`、排除 `DRAFT/REJECTED/PENDING_APPROVAL`），建议下个版本补修并补测试。

---

## 四、全流程闭环 E2E（68/68 通过）

以各角色真实登录（employee01/manager01/legal01/finance01/gm03），走 HTTP 全链路（含 CSRF、鉴权、数据范围、业务校验），每步落库断言。

### 场景 A：简易合同（≤1 万，仅部门经理）

`employee01 建客户 → 上传附件 → 建 8000 元合同 → 提交审批（自动匹配简易流）→ manager01 审批通过`

- ✅ 简易流程（flow=3）匹配正确，节点审批人 = manager01
- ✅ 合同状态 DRAFT → PENDING_APPROVAL → EXECUTING（审批通过后进入执行）
- ✅ 审批记录归属正确（无提交人自审）

### 场景 B：标准合同（0–10 万，法务 + 部门经理，含驳回重提）

`employee01 建 5 万合同 → 提交（flow=1）→ legal01 驳回 → 合同 REJECTED → 重提（新实例）→ legal01 通过 → manager01 通过`

- ✅ 标准流程节点 1=法务、节点 2=部门经理，逐节点推进正确
- ✅ 驳回后合同/实例状态均 REJECTED；重提生成新实例且节点 1 仍为法务
- ✅ 双节点通过后实例 APPROVED、合同 EXECUTING

### 场景 C：大额合同（10 万+，部门经理 + 财务会签）

`employee01 建 20 万合同 → 提交（flow=2）→ manager01 → finance01 会签通过`

- ✅ 大额流程节点 1=manager、节点 2=finance（AND 会签）
- ✅ 两节点通过后实例 APPROVED、合同 EXECUTING

### 场景 D：回款（部分确认）+ 发票（申请 → 审批 → 开票）

`finance01 登记 8 万回款计划 → 部分确认 5 万 → 自动拆分 3 万待收 → employee01 发票申请 3 万 → finance01 审批 → 财务开票`

- ✅ 回款计划 PENDING 落库；部分确认后母记录 PAID 5 万、自动拆分 3 万 PENDING 子记录
- ✅ 发票申请关联合同校验（金额不超合同额）、发票审批实例节点 1=财务
- ✅ 状态机：发票 PENDING_APPROVAL → APPROVED（待开票）→ ISSUED（填票号开票）

### 场景 E：权限门控抽查（8 项全过）

- ✅ employee01 访问 PC 周报 / 系统管理 → 403「权限不足」
- ✅ employee01 执行审批动作 → 403（无 approval:approve）
- ✅ manager01 访问 PC 周报 → 403（无 dashboard:company）
- ✅ gm03 可访问 PC 周报 / 移动周报 → 200 渲染正常
- ✅ gm03 有审批权限（对已办实例返回业务拒绝而非权限拒绝）

---

## 五、发布门禁

`scripts/run_gates.ps1` 六项静态检查（语法、路由入口、视图裸调用、前端加载顺序等）+ PHPUnit：**全部通过（65 tests / 172 assertions）**。

---

## 六、结论与建议

1. **整体质量**：多角色全场景全流程功能正确，权限与数据范围隔离有效，统计口径在本次审查范围内准确。
2. **本次修复**：`dashboardSummary` 状态快照周期过滤 bug（v2.47.1 修复点）。
3. **建议（下版本）**：
   - 修复 `trend` 与 `project aggregate/payScope` 两处潜在口径偏差（见 3.3）；
   - 若发布 v2.47.1，需同步 `VERSION.md` / `CHANGELOG.md` / `config/version.php` 三处版本号并重新出包。

## 附：环境恢复

测试产生的全部临时数据（E2E 客户/合同/审批/回款/发票、临时用户 legal01/gm03、测试脚本）已清理，演示库恢复基线（6 用户 / 18 合同 / 12 客户），`php -l` 全绿，发布门禁通过。
